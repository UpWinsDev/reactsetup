import React from 'react'
import SideBar from '../../src/components/SideBar'

// import { Container } from './styles';

const Pacientes: React.FC = () => {
    return <SideBar children={<h1>Lista de Pacientes</h1>} />
}

export default Pacientes
